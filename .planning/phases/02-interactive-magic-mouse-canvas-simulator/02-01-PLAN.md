---
phase: 02-interactive-magic-mouse-canvas-simulator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
  - docs/css/style.css
  - docs/js/simulator.js
autonomous: true
requirements:
  - SIM-01
  - SIM-02

must_haves:
  truths:
    - "Visitor sees a photorealistic top-down Apple Magic Mouse surface rendered crisply on Retina displays without blurriness"
    - "Visitor can click, drag, or touch the mouse surface to produce glowing capacitive contact points with normalized coordinate readout"
    - "Canvas dynamically resizes to fit container while preserving aspect ratio and Retina sharpness"
  artifacts:
    - docs/js/simulator.js
    - docs/index.html
  key_links:
    - "docs/index.html includes docs/js/simulator.js script tag"
    - "simulator.js renders onto #mouse-canvas element"
---

<objective>
Build the core Canvas 2D virtual Magic Mouse surface, Retina display scaling system, and real-time capacitive touch tracking engine.

Purpose: Delivers the visual foundation of the interactive simulator where visitors see and interact with an authentic virtual Magic Mouse surface.
Output: `docs/js/simulator.js`, updated `docs/index.html` and `docs/css/style.css`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-interactive-magic-mouse-canvas-simulator/02-CONTEXT.md
@Sources/MagicTouch/TouchPoint.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add simulator section layout and canvas container to docs/index.html</name>
  <files>docs/index.html, docs/css/style.css</files>
  <read_first>.planning/phases/02-interactive-magic-mouse-canvas-simulator/02-CONTEXT.md</read_first>
  <action>
    In `docs/index.html`, locate the `#simulator` section:
    - Replace the placeholder card with an authentic interactive simulator stage:
      - Section header: Eyebrow badge *"Interactive Hardware Simulator"*, headline *"Feel the Hardware. Try Every Gesture."*, description explaining how MagicTouch reads raw capacitive touch data.
      - Dual-column or centered showcase container with frosted glass frame (`.glass-panel`).
      - Left/Top panel: Interactive Canvas container with `<canvas id="mouse-canvas">` and overlaid coordinate indicator tooltip.
      - Right/Bottom panel: Live telemetry card displaying Active Touches count, Normalized `(X, Y)`, and Touch Area/Pressure metrics matching `TouchPoint.swift`.
    In `docs/css/style.css`, add styling for canvas container, coordinate badges, and glowing pulse keyframes.
  </action>
  <verify>Open `docs/index.html` in browser or inspect HTML structure to verify `#mouse-canvas` and telemetry containers exist.</verify>
  <commit_msg>feat(phase-02): add simulator section layout and canvas container</commit_msg>
</task>

<task type="auto">
  <name>Task 2: Implement Magic Mouse Canvas renderer with Retina scaling</name>
  <files>docs/js/simulator.js</files>
  <read_first>Sources/MagicTouch/TouchPoint.swift</read_first>
  <action>
    Create `docs/js/simulator.js`:
    - Setup `MagicMouseSimulator` class.
    - Implement Retina display scaling using `window.devicePixelRatio` so lines and curves remain crisp on 2x/3x Apple Retina displays.
    - Render the Apple Magic Mouse body:
      - Elegant rounded pill geometry with standard Magic Mouse proportions (~1:2 aspect ratio).
      - Subtle white acrylic glass gradient fill with specular gloss curve.
      - Aluminum edge highlight with 1px bevel stroke.
      - Subtle Apple logo badge in lower center.
      - Translucent coordinate axis lines and touch sensing zone guide.
    - Implement `requestAnimationFrame` render loop for smooth 60fps animations.
  </action>
  <verify>Run `node -c docs/js/simulator.js` to ensure syntax is valid.</verify>
  <commit_msg>feat(phase-02): implement canvas magic mouse renderer with retina scaling</commit_msg>
</task>

<task type="auto">
  <name>Task 3: Implement capacitive touch tracking and particle contact glow</name>
  <files>docs/js/simulator.js, docs/index.html</files>
  <read_first>.planning/phases/02-interactive-magic-mouse-canvas-simulator/02-CONTEXT.md</read_first>
  <action>
    In `docs/js/simulator.js`:
    - Add mouse event listeners (`mousedown`, `mousemove`, `mouseup`, `mouseleave`) and touch event listeners (`touchstart`, `touchmove`, `touchend`, `touchcancel`).
    - Map viewport pointer coordinates to normalized Magic Mouse touch coordinates:
      - `normalizedX`: 0.0 (left) to 1.0 (right).
      - `normalizedY`: 0.0 (bottom) to 1.0 (top), matching macOS multitouch framework orientation in `TouchPoint.swift`.
    - For each active touch:
      - Render glowing electric cyan/blue capacitive contact circles with expanding pulse rings.
      - Render trailing touch particle trail during movement.
      - Display normalized coordinates beside the touch point.
    - Update live telemetry DOM elements (Active Touches, coordinates, touch phase).
    - Link `docs/js/simulator.js` in `docs/index.html` script imports.
  </action>
  <verify>Run `node -c docs/js/simulator.js` and verify script tag is present in `docs/index.html`.</verify>
  <commit_msg>feat(phase-02): implement capacitive touch tracking and telemetry display</commit_msg>
</task>

</tasks>

<verification>
- `docs/js/simulator.js` passes syntax check: `node -c docs/js/simulator.js`
- `docs/index.html` correctly includes `<script src="js/simulator.js"></script>`
- Canvas element `#mouse-canvas` renders with Retina resolution
- Touching/clicking canvas tracks coordinates and renders glowing touch circles
</verification>
