---
phase: 02-interactive-magic-mouse-canvas-simulator
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - docs/index.html
  - docs/css/style.css
  - docs/js/audio.js
  - docs/js/simulator.js
  - docs/js/main.js
autonomous: true
requirements:
  - SIM-03
  - SIM-04
  - SIM-05

must_haves:
  truths:
    - "Visitor can click preset tour buttons (Middle Click, Tip-Tap Right, 2-Finger Pinch, Spaces Swipe, 4-Finger Tap) and watch animated fingertip trajectories on the canvas"
    - "Visitor can click 'Auto-Tour' to automatically cycle through all gesture demonstrations"
    - "HUD overlay dynamically displays 'Recognized Gesture' and 'Simulated Action' badges with pulsing highlight when gestures trigger"
    - "Visitor can toggle procedural audio haptics on and off with sound wave icon reflecting state, defaulting to muted"
    - "Procedural Web Audio API generates crisp mechanical click/tap sounds without external audio files"
  artifacts:
    - docs/js/audio.js
    - docs/js/simulator.js
    - docs/index.html
  key_links:
    - "docs/js/audio.js is loaded in docs/index.html and called by simulator.js"
    - "simulator.js exposes tour player methods triggered by preset buttons"
---

<objective>
Build the procedural Web Audio haptics synthesizer (`audio.js`), automated gesture playback tour engine, and real-time recognition/action HUD feedback badges.

Purpose: Transforms the static canvas into an engaging interactive demo where visitors experience MagicTouch's gestures, simulated actions, and tactile feedback.
Output: `docs/js/audio.js`, updated `docs/js/simulator.js`, `docs/index.html`, `docs/css/style.css`, and `docs/js/main.js`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-interactive-magic-mouse-canvas-simulator/02-CONTEXT.md
@Sources/MagicTouch/GestureRecognizer.swift
@Sources/MagicTouch/ConfigurationStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement procedural Web Audio haptics engine (audio.js)</name>
  <files>docs/js/audio.js</files>
  <read_first>.planning/phases/02-interactive-magic-mouse-canvas-simulator/02-CONTEXT.md</read_first>
  <action>
    Create `docs/js/audio.js`:
    - Implement `HapticAudio` module/class using Web Audio API (`AudioContext`).
    - Sound synthesis methods:
      - `playClick()`: Fast exponential frequency sweep (e.g., 240Hz down to 60Hz over 35ms) with high-pass filtered white noise burst (10ms) replicating Apple Taptic Engine mechanical click.
      - `playTap()`: Higher pitch short transient (400Hz down to 120Hz over 20ms) for light fingertip taps.
      - `playSwipe()`: Soft subtle noise whoosh with bandpass filter for fluid slide motions.
      - `playPinch()`: Dual micro-clicks in quick succession (15ms gap) for pinch recognition.
    - Mute state handling:
      - Default state: `isMuted = true` (browser autoplay compliance).
      - `toggleMute()`: Inverts mute state, initializes or resumes `AudioContext` on user interaction, returns current state.
      - State broadcast / event emitter so UI buttons can reflect sound state.
  </action>
  <verify>Run `node -c docs/js/audio.js` to ensure clean syntax.</verify>
  <commit_msg>feat(phase-02): implement procedural web audio haptics synthesizer</commit_msg>
</task>

<task type="auto">
  <name>Task 2: Implement Gesture Tour player and animated finger paths</name>
  <files>docs/js/simulator.js</files>
  <read_first>Sources/MagicTouch/GestureRecognizer.swift</read_first>
  <action>
    In `docs/js/simulator.js`, integrate tour playback capabilities:
    - Define animated gesture paths with keyframe interpolations:
      1. **Middle Click (3-Finger Click)**: 3 fingers positioned in top third of mouse, press down with growing radius, trigger haptic click, release.
      2. **Tip-Tap Right**: 2 fingers resting on mouse; middle finger taps down while index remains stationary.
      3. **2-Finger Pinch In/Out**: 2 fingers start wide and converge inwards (Pinch In / Zoom Out) or diverge outwards (Pinch Out / Zoom In).
      4. **2-Finger Swipe**: 2 fingers gliding smoothly left-to-right or right-to-left across the upper surface.
      5. **4-Finger Tap**: 4 fingers tapping simultaneously across surface triggering Mission Control.
    - Implement `playGesture(gestureId)`:
      - Cancels ongoing manual touches or prior animations.
      - Runs timeline animation via `requestAnimationFrame` with smooth cubic bezier easing.
      - Fires haptic audio (`audio.playClick()`, `audio.playTap()`, etc.) at the exact trigger keyframe.
      - Dispatches recognition event with gesture details.
    - Implement `startAutoTour()` / `stopAutoTour()`:
      - Automatically cycles through all presets with 3-second dwell and progress indicator.
      - Stops automatically if visitor manually clicks/touches the canvas.
  </action>
  <verify>Run `node -c docs/js/simulator.js` to ensure syntax is valid.</verify>
  <commit_msg>feat(phase-02): implement gesture tour animation engine and auto-tour cycler</commit_msg>
</task>

<task type="auto">
  <name>Task 3: Implement HUD badges, tour preset bar, and audio toggle UI</name>
  <files>docs/index.html, docs/css/style.css, docs/js/main.js</files>
  <read_first>.planning/phases/02-interactive-magic-mouse-canvas-simulator/02-CONTEXT.md</read_first>
  <action>
    In `docs/index.html`:
    - Add the interactive preset bar with pill buttons:
      - "3-Finger Click (Middle Click)"
      - "Tip-Tap Right"
      - "2-Finger Pinch"
      - "Spaces Swipe"
      - "4-Finger Tap"
      - "✨ Auto-Tour" button with pulsing status ring.
    - Add the Sound Toggle button (`#sound-toggle`) with mute/unmute SVG icons.
    - Add the Recognition HUD overlay (`#recognition-hud`):
      - "Recognized Gesture": Dynamic badge with gesture icon and title (e.g., "Middle Click (3-Finger)").
      - "Simulated Action": Dynamic badge showing OS action (e.g., "Mouse Button 3 / Open Link in Tab").
      - Latency & Dispatch indicator: "Direct Multitouch Event (<1ms)".
    In `docs/css/style.css`:
    - Add HUD trigger pulse animation (`hud-pulse`), active preset button highlight, and sound toggle styling.
    In `docs/js/main.js`:
    - Wire preset buttons to `simulator.playGesture(id)`.
    - Wire Auto-Tour button to toggle `simulator.toggleAutoTour()`.
    - Wire Sound Toggle button to `HapticAudio.toggleMute()`.
    - Listen for `gestureRecognized` events from simulator to update HUD badges with active pulse effect.
    - Add script tag for `js/audio.js` before `js/simulator.js` in `docs/index.html`.
  </action>
  <verify>Verify `docs/index.html` contains all preset buttons, audio toggle, and HUD elements. Run `node -c docs/js/main.js`.</verify>
  <commit_msg>feat(phase-02): add recognition HUD, preset controls, and sound toggle UI</commit_msg>
</task>

</tasks>

<verification>
- `docs/js/audio.js` passes syntax check: `node -c docs/js/audio.js`
- `docs/js/simulator.js` passes syntax check: `node -c docs/js/simulator.js`
- `docs/js/main.js` passes syntax check: `node -c docs/js/main.js`
- Clicking any preset pill runs the finger animation on the canvas and triggers HUD recognition badge
- Clicking sound toggle unmutes audio and plays haptic feedback on gestures
</verification>
