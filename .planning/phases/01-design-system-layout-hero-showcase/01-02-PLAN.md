---
phase: 01-design-system-layout-hero-showcase
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - docs/index.html
  - docs/js/main.js
autonomous: true
requirements:
  - HERO-02
  - HERO-03
  - HERO-04

must_haves:
  truths:
    - "Primary 'Download for macOS' CTA button links to universal MagicTouch.dmg installer directly from GitHub Releases"
    - "JavaScript asynchronously checks GitHub Releases API and dynamically updates version tag and asset info with static fallback (v0.1.1)"
    - "Visitor can click one-click copy button on terminal command snippet and receive immediate 'Copied!' confirmation"
  artifacts:
    - docs/index.html
    - docs/js/main.js
  key_links:
    - "docs/index.html loads docs/js/main.js which populates release tags and handles clipboard actions"
---

<objective>
Implement the dynamic GitHub Releases download integration, version tag updates, dual secondary CTA actions, and interactive one-click copyable terminal install snippet.

Purpose: Delivers a high-conversion download experience connected to GitHub Releases with zero-friction developer setup.
Output: `docs/js/main.js`, updated `docs/index.html`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md
@.planning/phases/01-design-system-layout-hero-showcase/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add primary glowing download CTA, secondary actions, and trust footnote</name>
  <files>docs/index.html</files>
  <read_first>docs/index.html, .planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md</read_first>
  <action>
    In `docs/index.html` under the Hero headline, add:
    - Primary Download Button (`#primary-download-btn`):
      - Glowing Apple Blue pill button (`bg-[#0071e3] hover:bg-[#0077ed] text-white font-medium shadow-lg shadow-blue-500/25 transition-all transform hover:-translate-y-0.5`).
      - Apple SVG icon + primary label "Download for macOS".
      - Subtext span (`#download-subtext`): "v0.1.1 Universal • DMG".
      - Default link target: `https://github.com/namikemen/magictouch/releases/latest/download/MagicTouch.dmg`.
    - Secondary Action Buttons group:
      - "Try Interactive Simulator" button linking to `#simulator` with down-arrow icon.
      - "GitHub" button linking to `https://github.com/namikemen/magictouch` with GitHub SVG icon.
    - Trust & Compatibility footnote:
      - Small muted caption: "Free & Open Source under MIT • Universal Binary (Apple Silicon & Intel) • macOS 13+ • Verified SHA-256".
  </action>
  <verify>grep -q "primary-download-btn" docs/index.html && grep -q "MagicTouch.dmg" docs/index.html</verify>
  <acceptance_criteria>
    - `docs/index.html` contains `#primary-download-btn` linking to `MagicTouch.dmg`
    - `docs/index.html` contains `#download-subtext`
    - `docs/index.html` contains secondary buttons for Simulator and GitHub
    - `docs/index.html` contains MIT / Universal Binary trust footnote
  </acceptance_criteria>
  <done>Primary download CTA, secondary actions, and trust metadata added to Hero section in `docs/index.html`.</done>
</task>

<task type="auto">
  <name>Task 2: Add sleek developer terminal install snippet with copy button</name>
  <files>docs/index.html</files>
  <read_first>docs/index.html, .planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md</read_first>
  <action>
    In `docs/index.html` below the CTA buttons, add a developer terminal snippet card (`#terminal-snippet`):
    - Container styled with `.glass-panel rounded-xl max-w-xl mx-auto px-4 py-3 flex items-center justify-between font-mono text-sm`.
    - Command text: `git clone https://github.com/namikemen/magictouch.git && cd magictouch && ./package_app.sh`
    - Copy Button (`#copy-command-btn`):
      - Copy SVG icon with tooltip/label "Copy".
      - Feedback container (`#copy-feedback`) that displays an animated checkmark and "Copied!" text upon click.
  </action>
  <verify>grep -q "copy-command-btn" docs/index.html && grep -q "package_app.sh" docs/index.html</verify>
  <acceptance_criteria>
    - `docs/index.html` contains terminal snippet card with git clone / package_app command
    - `docs/index.html` contains `#copy-command-btn`
    - `docs/index.html` contains `#copy-feedback` element
  </acceptance_criteria>
  <done>Developer terminal command block with copy button integrated into `docs/index.html`.</done>
</task>

<task type="auto">
  <name>Task 3: Create main.js with GitHub Releases integration and copy-to-clipboard handler</name>
  <files>docs/js/main.js, docs/index.html</files>
  <read_first>.planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md</read_first>
  <action>
    Create `docs/js/main.js` containing:
    1. GitHub Releases Fetcher (`initReleaseData`):
       - Fallback constants: `DEFAULT_VERSION = 'v0.1.1'`, `DEFAULT_DMG_URL = 'https://github.com/namikemen/magictouch/releases/latest/download/MagicTouch.dmg'`.
       - Query GitHub Releases API: `https://api.github.com/repos/namikemen/magictouch/releases/latest`.
       - If successful, extract `tag_name` and find the asset matching `.dmg` to extract size in MB.
       - Update DOM elements (`#version-tag`, `#download-subtext`, `#release-version-badge`) with current version and formatted file size (e.g. "v0.1.1 Universal • 8.4 MB DMG").
       - Graceful error catch: If request fails (e.g. offline, rate limit), keep the default static fallback without showing any error alert.
    2. Clipboard Copy Handler (`initClipboard`):
       - Attach click listener to `#copy-command-btn`.
       - Copy command text to `navigator.clipboard.writeText(...)`.
       - Show `#copy-feedback` with green checkmark for 2.5 seconds, then reset.
    3. Mobile Menu Controller (`initMobileMenu`):
       - Attach click listener to `#mobile-menu-btn` to toggle `#mobile-menu` visibility with smooth opacity and slide animation.
       - Close menu when any mobile navigation link is clicked or clicking the backdrop overlay.
    4. Link `docs/js/main.js` in `docs/index.html` before closing `</body>` tag (`<script src="js/main.js" defer></script>`).
  </action>
  <verify>test -f docs/js/main.js && grep -q "initReleaseData" docs/js/main.js && grep -q "initClipboard" docs/js/main.js</verify>
  <acceptance_criteria>
    - `docs/js/main.js` exists
    - `docs/js/main.js` implements GitHub Releases API fetching with fallback
    - `docs/js/main.js` implements clipboard copy with visual feedback
    - `docs/js/main.js` handles mobile menu drawer open/close
    - `docs/index.html` includes `<script src="js/main.js" defer></script>`
  </acceptance_criteria>
  <done>`docs/js/main.js` implemented and wired to `docs/index.html` with GitHub API fetching, copy-to-clipboard, and mobile menu interaction.</done>
</task>

</tasks>

<verification>
1. Verify `docs/js/main.js` runs cleanly without JavaScript syntax errors (`node -c docs/js/main.js`).
2. Verify `docs/index.html` contains download button `#primary-download-btn` and terminal copy button `#copy-command-btn`.
3. Test terminal copy interaction and verify "Copied!" feedback toggles properly.
4. Test release updater logic with mock/live API response.
</verification>
