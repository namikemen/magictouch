---
phase: 01-design-system-layout-hero-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
  - docs/css/style.css
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - HERO-01

must_haves:
  truths:
    - "Visitor opens docs/index.html and sees deep obsidian background (#070709) with frosted glass panels, subtle electric blue/cyan ambient glows, and 1px translucent borders"
    - "Sticky floating navigation pill is centered at top of viewport with backdrop blur, brand logo, navigation links, GitHub star button, and mobile hamburger drawer"
    - "Hero section presents bold headline 'Unleash Your Magic Mouse', platform badges, and floating native macOS popover preview window"
  artifacts:
    - docs/index.html
    - docs/css/style.css
  key_links:
    - "docs/index.html links to docs/css/style.css and loads Tailwind CSS utilities"
---

<objective>
Establish the zero-build static site structure in `docs/` with dark glassmorphic styling, responsive layout system, floating frosted glass navigation bar, and the centered spotlight Hero section.

Purpose: Creates the core visual foundation and branding for the MagicTouch product showcase on GitHub Pages.
Output: `docs/index.html`, `docs/css/style.css`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md
@Sources/MagicTouch/Views/MainPopoverView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize docs structure and HTML5 document skeleton</name>
  <files>docs/index.html</files>
  <read_first>.planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md</read_first>
  <action>
    Create `docs/index.html` with:
    - HTML5 doctype, UTF-8 charset, responsive viewport (`width=device-width, initial-scale=1.0`).
    - SEO title: "MagicTouch 🪄🖱️ — Ultra-Responsive Magic Mouse Gestures for macOS".
    - Meta tags for description ("A native macOS utility unlocking trackpad-class gestures, middle click, pinches, and hotkeys on Apple Magic Mouse").
    - OpenGraph and Twitter card metadata for rich previews.
    - Script tag importing Tailwind CSS via CDN (`https://cdn.tailwindcss.com`).
    - Link tag loading `css/style.css`.
    - Native Apple font family styling (`font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", "Inter", sans-serif`).
    - Semantic container layout (`header`, `main`, `footer`) wrapped in deep obsidian background (`#070709`).
  </action>
  <verify>test -f docs/index.html && grep -q "MagicTouch" docs/index.html</verify>
  <acceptance_criteria>
    - `docs/index.html` exists
    - `docs/index.html` contains `cdn.tailwindcss.com`
    - `docs/index.html` contains `css/style.css`
    - `docs/index.html` contains `viewport` meta tag
  </acceptance_criteria>
  <done>HTML5 document skeleton created in `docs/index.html` with proper SEO, Tailwind, and styling links.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dark glassmorphism styling system</name>
  <files>docs/css/style.css</files>
  <read_first>.planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md</read_first>
  <action>
    Create `docs/css/style.css` containing:
    - Root CSS variables for colors:
      - `--bg-obsidian: #070709;`
      - `--card-glass: rgba(18, 18, 23, 0.65);`
      - `--border-glass: rgba(255, 255, 255, 0.08);`
      - `--accent-blue: #0071e3;`
      - `--accent-cyan: #38bdf8;`
    - Glassmorphism utility classes (`.glass-panel`, `.glass-pill`, `.glass-card`):
      - `backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);`
      - `background: var(--card-glass);`
      - `border: 1px solid var(--border-glass);`
      - Specular top-edge gradient highlight (`box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.12);`).
    - Glow utilities (`.glow-blue`, `.glow-cyan`, `.radial-glow-bg`):
      - Subtle radial ambient light gradients that don't cause GPU stutter.
    - Smooth scrolling behavior on `html { scroll-behavior: smooth; }`.
    - Keyframe animation for subtle pulse and badge glows (`@keyframes pulse-subtle`).
  </action>
  <verify>test -f docs/css/style.css && grep -q "glass-panel" docs/css/style.css</verify>
  <acceptance_criteria>
    - `docs/css/style.css` exists
    - `docs/css/style.css` defines `.glass-panel` and `.glass-pill`
    - `docs/css/style.css` contains `backdrop-filter: blur(`
    - `docs/css/style.css` contains `--accent-blue: #0071e3`
  </acceptance_criteria>
  <done>Glassmorphic CSS styling system created with frosted cards, specular rim highlights, and ambient glow utilities.</done>
</task>

<task type="auto">
  <name>Task 3: Build floating frosted glass navigation bar with mobile menu</name>
  <files>docs/index.html</files>
  <read_first>docs/css/style.css, .planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md</read_first>
  <action>
    In `docs/index.html`, implement the navigation header:
    - Sticky floating capsule centered at top: `sticky top-4 z-50 mx-auto max-w-5xl px-4 sm:px-6`.
    - Nav inner container with `.glass-pill rounded-full px-5 py-3 flex items-center justify-between`.
    - Brand section: MagicTouch icon (`🪄🖱️` or SVG) + text "MagicTouch" with font-semibold.
    - Center navigation links: `#features`, `#simulator`, `#gestures`, `#installation` with subtle hover opacity transitions.
    - Right quick action group:
      - GitHub repository link with GitHub SVG logo.
      - Compact "Download .DMG" pill button with `#download` anchor.
    - Mobile hamburger button (`lg:hidden`) toggling a sliding frosted drawer (`#mobile-menu`) with backdrop overlay.
  </action>
  <verify>grep -q "glass-pill" docs/index.html && grep -q "mobile-menu" docs/index.html</verify>
  <acceptance_criteria>
    - `docs/index.html` contains floating navigation pill with `.glass-pill`
    - `docs/index.html` contains navigation links `#features`, `#simulator`, `#gestures`, `#installation`
    - `docs/index.html` contains mobile menu toggle and drawer
  </acceptance_criteria>
  <done>Floating frosted navigation pill and mobile drawer integrated into `docs/index.html`.</done>
</task>

<task type="auto">
  <name>Task 4: Build centered spotlight Hero section and floating macOS popover preview</name>
  <files>docs/index.html</files>
  <read_first>Sources/MagicTouch/Views/MainPopoverView.swift, .planning/phases/01-design-system-layout-hero-showcase/01-CONTEXT.md</read_first>
  <action>
    In `docs/index.html`, implement the Hero section (`#hero`):
    - Ambient radial glow background element (`.radial-glow-bg`).
    - Platform pill badges row:
      - "⚡ Universal Binary (Apple Silicon & Intel)"
      - "🖥️ macOS 13+ Ventura / Sonoma / Sequoia"
      - "🔒 Zero Telemetry • 100% Local"
      - "✨ v0.1.1 Released" (with pulse green dot)
    - Hero headline:
      - `h1`: "Unleash Your Magic Mouse" with gradient text highlight.
      - `p`: "Ultra-responsive multi-touch gestures, middle click, pinches, and custom hotkeys for macOS with sub-millisecond latency."
    - Anchor placeholder for the download CTA buttons (to be wired to dynamic API in Plan 01-02).
    - Floating Native macOS Popover Preview Card (`.glass-panel rounded-2xl`):
      - macOS window chrome header with red, yellow, green window dots.
      - Menu bar status item badge (`magicmouse.fill` icon with "MagicTouch", green connection dot "Apple Magic Mouse Connected").
      - Mini visualizer frame representation showing touch surface and gesture detection badge ("Detected: 3-Finger Click → Middle Click").
      - Quick toggle switch simulation ("Active").
  </action>
  <verify>grep -q "Unleash Your Magic Mouse" docs/index.html && grep -q "Universal Binary" docs/index.html</verify>
  <acceptance_criteria>
    - `docs/index.html` contains `Unleash Your Magic Mouse` headline
    - `docs/index.html` contains platform badges row
    - `docs/index.html` contains floating macOS popover preview mockup
  </acceptance_criteria>
  <done>Centered spotlight Hero section and floating macOS popover preview mockup built in `docs/index.html`.</done>
</task>

</tasks>

<verification>
1. `docs/index.html` and `docs/css/style.css` exist and are populated.
2. Open `docs/index.html` in browser or serve locally to verify responsive layout, glassmorphic blur, specular highlights, and typography.
3. Test mobile menu hamburger toggle on narrow viewports.
</verification>
