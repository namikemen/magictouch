---
phase: 03-gesture-matrix-installation-guide-github-pages-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - docs/index.html
  - docs/css/style.css
  - docs/favicon.svg
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02
  - DPLY-01
  - DPLY-02

must_haves:
  truths:
    - "Visitor sees technical architecture cards explaining private framework interception, zero background telemetry, and native Swift/AppKit efficiency"
    - "Visitor can follow a visual 3-step walkthrough for granting macOS Accessibility and Input Monitoring permissions with interactive toggle mockups"
    - "Site includes modern SVG favicon with Magic Mouse & wand motif, Apple touch icon, and OpenGraph/Twitter preview tags"
    - "docs/ directory is 100% self-contained with zero build dependencies, ready for instant GitHub Pages deployment"
  artifacts:
    - docs/favicon.svg
    - docs/index.html
  key_links:
    - "docs/index.html links to docs/favicon.svg"
    - "Zero 404s or broken relative links across entire docs/ directory"
---

<objective>
Deliver the technical architecture highlights, visual macOS permissions walkthrough, favicon/SEO assets, and verify GitHub Pages production readiness.

Purpose: Completes the remaining product showcase requirements so new users know exactly how to install and configure MagicTouch on macOS.
Output: `docs/favicon.svg`, updated `docs/index.html`, and deployment verification report.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-gesture-matrix-installation-guide-github-pages-readiness/03-CONTEXT.md
@Sources/MagicTouch/Engine/PermissionManager.swift
@Sources/MagicTouch/Views/PermissionBannerView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build technical architecture highlights section in docs/index.html</name>
  <files>docs/index.html, docs/css/style.css</files>
  <read_first>Sources/MagicTouch/Engine/MultitouchManager.swift</read_first>
  <action>
    In `docs/index.html`, replace `#features` section with a technical architecture showcase:
    - Section header: Eyebrow *"Engineering & Architecture"*, headline *"Designed for Precision. Built for macOS."*
    - 3 glassmorphic architecture cards:
      1. **Sub-Millisecond Private C-API Tap**: Direct hook into macOS `MultitouchSupport.framework` reading raw capacitive surface sensor arrays at hardware polling rates.
      2. **Privacy First & Zero Telemetry**: 100% offline, zero network tracking, no analytics, no ads. Distributed as an audited open-source binary under MIT license.
      3. **Native Swift & AppKit Core**: Zero electron bloat, consumes under 25MB RAM, ~0% idle CPU footprint, native macOS menu bar status item.
  </action>
  <verify>Verify `#features` section in `docs/index.html` contains the 3 architecture cards.</verify>
  <commit_msg>feat(phase-03): build technical architecture highlights section</commit_msg>
</task>

<task type="auto">
  <name>Task 2: Build visual macOS permissions walkthrough in docs/index.html</name>
  <files>docs/index.html, docs/css/style.css</files>
  <read_first>Sources/MagicTouch/Engine/PermissionManager.swift</read_first>
  <action>
    In `docs/index.html`, replace `#installation` section with a visual 3-step setup guide:
    - Step 1: **Download & Install**: Drag `MagicTouch.app` into `/Applications` folder.
    - Step 2: **Accessibility Permission**:
      - Path: `System Settings → Privacy & Security → Accessibility`.
      - Purpose: Allows MagicTouch to synthesize native mouse clicks, middle clicks, and hotkeys.
      - Mock macOS UI toggle card with interactive switch.
    - Step 3: **Input Monitoring Permission**:
      - Path: `System Settings → Privacy & Security → Input Monitoring`.
      - Purpose: Allows reading raw capacitive multitouch coordinates from the Magic Mouse.
      - Mock macOS UI toggle card with interactive switch.
    - Add troubleshooting note for macOS Sequoia / Sonoma permission resets.
  </action>
  <verify>Verify `#installation` section in `docs/index.html` contains the 3-step guide and interactive mock toggles.</verify>
  <commit_msg>feat(phase-03): build visual macOS permissions walkthrough guide</commit_msg>
</task>

<task type="auto">
  <name>Task 3: Create SVG favicon, finalize SEO metadata, and verify GitHub Pages serving</name>
  <files>docs/favicon.svg, docs/index.html</files>
  <read_first>.planning/phases/03-gesture-matrix-installation-guide-github-pages-readiness/03-CONTEXT.md</read_first>
  <action>
    - Create `docs/favicon.svg` with sleek Magic Mouse outline and magic sparkle wand star motif in Apple electric blue and cyan.
    - Add favicon link tags to `docs/index.html`:
      - `<link rel="icon" type="image/svg+xml" href="favicon.svg">`
      - `<link rel="apple-touch-icon" href="favicon.svg">`
    - Verify all asset paths in `docs/` are relative (`css/style.css`, `js/*.js`, `favicon.svg`).
    - Run local HTTP server check (`python3 -m http.server --directory docs`) to ensure clean 200 responses on all static resources.
  </action>
  <verify>Check `docs/favicon.svg` exists, verify HTML links, test HTTP response codes on all files.</verify>
  <commit_msg>feat(phase-03): add SVG favicon, complete SEO metadata, and verify deployment</commit_msg>
</task>

</tasks>

<verification>
- `docs/favicon.svg` renders cleanly
- All script tags load without 404s
- Python static server serves `docs/` with all resources returning HTTP 200
- Entire website in `docs/` is self-contained with zero build commands needed for GitHub Pages
</verification>
