---
phase: 03-gesture-matrix-installation-guide-github-pages-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
  - docs/css/style.css
  - docs/js/gestures-data.js
  - docs/js/gestures.js
autonomous: true
requirements:
  - MTRX-01
  - MTRX-02
  - MTRX-03
  - GUIDE-03

must_haves:
  truths:
    - "Visitor can browse all 30+ native MagicTouch gestures categorized by finger count (1, 2, 3, and 4 fingers)"
    - "Visitor can filter gestures via category pill buttons or search gestures in real time via instant search input"
    - "Each gesture card displays name, finger count badge, icon, description, and default action target badge"
    - "Action target breakdown highlights Mouse Clicks, Hotkeys, macOS System Navigation, and Shell/AppleScript scripts"
  artifacts:
    - docs/js/gestures-data.js
    - docs/js/gestures.js
    - docs/index.html
  key_links:
    - "docs/index.html loads docs/js/gestures-data.js and docs/js/gestures.js"
    - "gestures.js renders cards into #gesture-grid container"
---

<objective>
Implement the filterable 30+ gesture matrix, live search filter, action targets showcase, and popover preview in `docs/index.html`.

Purpose: Empowers visitors to explore the complete catalog of MagicTouch gestures and understand how each can be bound to arbitrary macOS actions.
Output: `docs/js/gestures-data.js`, `docs/js/gestures.js`, updated `docs/index.html` and `docs/css/style.css`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-gesture-matrix-installation-guide-github-pages-readiness/03-CONTEXT.md
@Sources/MagicTouch/Models/GestureModel.swift
@Sources/MagicTouch/Store/ConfigurationStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structured gesture catalog dataset (docs/js/gestures-data.js)</name>
  <files>docs/js/gestures-data.js</files>
  <read_first>Sources/MagicTouch/Models/GestureModel.swift</read_first>
  <action>
    Create `docs/js/gestures-data.js` exporting `window.GESTURES_DATA`:
    - Array of all 41 gestures from `GestureModel.swift`:
      - 1-Finger: Tap Left, Tap Right, Double Tap, Double Tap Left/Right, Triple Tap, Triple Tap Left/Right, Click, Swipe Left/Right/Up/Down.
      - 2-Finger: Tap, Double Tap, Triple Tap, Click, Swipe Left/Right/Up/Down, Pinch In, Pinch Out, Tip-Tap Left, Tip-Tap Right.
      - 3-Finger: Tap, Double Tap, Triple Tap, Click (Middle Click), Swipe Left/Right/Up/Down, Pinch In, Pinch Out.
      - 4-Finger: Tap, Click, Swipe Left/Right/Up/Down.
    - Each record has: `id`, `name`, `fingerCount`, `category` (`'tap'`, `'click'`, `'swipe'`, `'pinch'`, `'tip-tap'`), `icon`, `description`, `defaultAction`, and `actionType` (`'mouse'`, `'hotkey'`, `'system'`, `'script'`).
  </action>
  <verify>Run `node -c docs/js/gestures-data.js`.</verify>
  <commit_msg>feat(phase-03): create structured gesture catalog dataset</commit_msg>
</task>

<task type="auto">
  <name>Task 2: Implement dynamic gesture grid filtering and search (docs/js/gestures.js)</name>
  <files>docs/js/gestures.js</files>
  <read_first>.planning/phases/03-gesture-matrix-installation-guide-github-pages-readiness/03-CONTEXT.md</read_first>
  <action>
    Create `docs/js/gestures.js`:
    - State: `activeCategory = 'all'`, `searchQuery = ''`.
    - Function `renderGestures()`:
      - Filters `window.GESTURES_DATA` based on active category and search query.
      - Renders frosted glass gesture cards (`.glass-panel`) into `#gesture-grid`.
      - Updates result count badge: e.g. "Showing 41 of 41 gestures".
      - Shows clean empty state if no gestures match search query.
    - Event listeners for category filter pills and search input with instant debounced input.
  </action>
  <verify>Run `node -c docs/js/gestures.js`.</verify>
  <commit_msg>feat(phase-03): implement gesture filtering and live search</commit_msg>
</task>

<task type="auto">
  <name>Task 3: Build gesture matrix section and action targets grid in docs/index.html</name>
  <files>docs/index.html, docs/css/style.css</files>
  <read_first>.planning/phases/03-gesture-matrix-installation-guide-github-pages-readiness/03-CONTEXT.md</read_first>
  <action>
    In `docs/index.html`, replace `#gestures` section:
    - Section header: Eyebrow *"Comprehensive Gesture Matrix"*, headline *"Over 40 Gestures. Infinite Possibilities."*, description.
    - Filter controls: Filter pill bar (All, 1 Finger, 2 Fingers, 3 Fingers, 4 Fingers, Tip-Taps, Pinches) and instant search bar.
    - Gesture Cards Grid: Responsive container (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`) with `#gesture-grid`.
    - Action Targets Grid: 4 cards below matrix:
      1. Mouse Button Remapping (Middle Click Button 3, Double/Triple Click, Back/Forward Buttons).
      2. Keyboard Shortcuts (Record any custom hotkey with ⌘, ⌥, ⌃, ⇧).
      3. macOS System Features (Mission Control, App Exposé, Spaces, Smart Zoom).
      4. Scripts & Automations (AppleScript and Shell commands).
    - Import `<script src="js/gestures-data.js" defer></script>` and `<script src="js/gestures.js" defer></script>`.
  </action>
  <verify>Verify `docs/index.html` loads scripts and contains `#gesture-grid`.</verify>
  <commit_msg>feat(phase-03): build gesture matrix layout and action targets grid</commit_msg>
</task>

</tasks>

<verification>
- `node -c docs/js/gestures-data.js` and `node -c docs/js/gestures.js` exit with code 0
- Category pills filter cards by finger count and type
- Search input filters cards dynamically by keywords
- Action target cards present all four configuration types
</verification>
